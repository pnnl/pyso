# Archive Models

This folder includes previously generated market models for day-ahead
and real-time markets as well as a specific TSO to coordinate between
the two.

These elements are in the process of being re-factored and included in
the existing `market.py` and `run_tso.py` scripts. These still may be used for
testing and certain functionality will be retained in future versions
of the `marketmodels` folder.